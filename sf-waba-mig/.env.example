# Opcional: só necessário se você não quiser passar --input/--json/--output
# toda vez. Argumentos de linha de comando, quando informados, sempre têm
# prioridade sobre estas variáveis.

INPUT_DIR=./messaging_components_v1
JSON_PATH=./templates.json
OUTPUT_DIR=./messaging_components_v2
